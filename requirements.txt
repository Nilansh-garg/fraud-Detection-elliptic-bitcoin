# ── NOTE ─────────────────────────────────────────────────────────────────────
# torch, torch-scatter, torch-sparse, torch-cluster, torch-spline-conv,
# and torch-geometric are ALL installed in the Dockerfile with pinned versions.
# DO NOT add torch here — it will conflict with the Dockerfile install and
# silently break PyG (torch-geometric depends on exact torch ABI).
# ─────────────────────────────────────────────────────────────────────────────

# Data & ML
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.13.0

# MLOps & Tracking
mlflow==2.13.0
dagshub
dvc

# Web Framework
flask==3.0.3
gunicorn==22.0.0
requests==2.32.2

# Visualization
matplotlib==3.9.0
seaborn==0.13.2

# Model download from HF Hub at runtime (same pattern as kidney project)
huggingface_hub==0.23.2

# ── REMOVED: -e . ─────────────────────────────────────────────────────────────
# "-e ." installs the current directory as an editable package and requires
# setup.py or pyproject.toml to exist. HF Spaces build will crash if those
# files are missing. If you have a proper package setup, add it back.
# ─────────────────────────────────────────────────────────────────────────────
